---
- hosts: CRM_Database
  gather_facts: true
  any_errors_fatal: true

  vars:
        sap_sid: "{{ crm_sid }}"
  vars_files:
    - vars/windows.yaml
    - vars/local_vars.yaml

  tasks: 
  - include_role:
      name: data
      tasks_from: main