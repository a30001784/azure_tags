---
- hosts: ISU_Database
  gather_facts: true
  any_errors_fatal: true

  vars_files:
    - "vars/windows.yaml"
  
  tasks: 

  - include_role:
      name: isu_db_bootstrap
      tasks_from: crm_db_bootstrap_01